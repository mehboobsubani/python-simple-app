apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "python-simple-app.fullname" . }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "python-simple-app.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
